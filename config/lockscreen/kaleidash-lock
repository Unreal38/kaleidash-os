#!/usr/bin/env bash
set -Eeuo pipefail

if command -v noctalia >/dev/null 2>&1; then
  if noctalia msg session lock; then
    exit 0
  fi
fi

printf 'KaleidashOS: Noctalia lock was unavailable; falling back to swaylock.\n' >&2
exec swaylock "$@"
